apiVersion: v1
kind: Service
metadata:
  name: bulletin-board
  namespace: edu
  labels:
    app: bulletin-board
spec:
  type: LoadBalancer
  selector:
    app: bulletin-board
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8080
